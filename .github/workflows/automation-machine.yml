name: Beast Machine - Final Secured Version

on:
  workflow_dispatch:
  repository_dispatch:
    types: [ouroboros_trigger]

concurrency:
  group: beast-machine-lock

jobs:
  beast-loop:
    runs-on: ubuntu-latest

    steps:
      - name: "১. জাভা ২১ ইনস্টল (Adoptium Method)"
        run: |
          wget -qO - https://packages.adoptium.net/artifactory/api/gpg/key/public | gpg --dearmor | sudo tee /etc/apt/trusted.gpg.d/adoptium.gpg > /dev/null
          echo "deb https://packages.adoptium.net/artifactory/deb $(awk -F= '/^VERSION_CODENAME/{print $2}' /etc/os-release) main" | sudo tee /etc/apt/sources.list.d/adoptium.list
          sudo apt update -y
          sudo apt install temurin-21-jre -y
          sudo update-alternatives --set java /usr/lib/jvm/temurin-21-jre-amd64/bin/java
          java -version

      - name: "২. Rclone ইনস্টল ও নিরাপদ কনফিগ সেটআপ"
        run: |
          curl https://rclone.org/install.sh | sudo bash
          sudo mkdir -p /root/.config/rclone
          
          # এখানে সরাসরি টোকেন না দিয়ে সিক্রেট ব্যবহার করা হচ্ছে
          sudo bash -c "cat << 'EOF' > /root/.config/rclone/rclone.conf
          ${{ secrets.RCLONE_CONFIG }}
          EOF"
          
          sudo chmod 600 /root/.config/rclone/rclone.conf
          echo "Rclone Config setup completed safely!"

      - name: "৩. Tailscale ও SSH রুট এক্সেস"
        run: |
          curl -fsSL https://tailscale.com/install.sh | sh
          sudo tailscale up --authkey=${{ secrets.TAILSCALE_AUTHKEY }} --hostname=beast-machine
          echo "root:root" | sudo chpasswd
          sudo sed -i 's/PermitRootLogin prohibit-password/PermitRootLogin yes/g' /etc/ssh/sshd_config
          sudo systemctl restart ssh

      - name: "৪. ৫ মিনিটের টেস্টিং উইন্ডো"
        run: |
          echo "মেশিন রেডি। rclone lsd gdrive: দিয়ে চেক করুন।"
          sleep 300 

      - name: "৫. লুপ ট্রিগার"
        run: |
          curl -X POST \
            -H "Authorization: token ${{ secrets.GH_PAT }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/repos/${{ github.repository }}/dispatches \
            -d '{"event_type": "ouroboros_trigger"}'
            
